#include <bits/stdc++.h>
using namespace std;

class Node
{
public:
    Node(int d)
    {
        int data = d;
        Node *l = NULL;
        Node *r = NULL;
    }
};

class Tree
{
public:
    Node *root = NULL;

    void push(int d)
    {
        // if (root == NULL)
        // {
        //     Node *nn = new Node(d);
        // }
    }
};
int main()
{
}