#include <bits/stdc++.h>
 
using namespace std;
 
int main()
{
    // test cases
    int t;
    cin>>t;
    while (t--)
    {
        // sizes of the mall, numver of vika's friends
        int m,n,k;
        cin >> m>>n>>k;
        // vika's locations
        int x,y;
        cin >> x>> y;
        while (k--)
        {
            int a,b; // each friend's location
            cin>>a>>b;
        }
    }
}

// O(n O(1 * O(1))) + O(1) = O(n)